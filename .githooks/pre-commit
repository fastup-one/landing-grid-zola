#!/usr/bin/env bash
# Fast pre-commit gate subset (no browser/docker). Enable once per clone with:
#   git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "▶ pre-commit gates (invariants · links · zola check · actionlint · gitleaks)"
exec make precommit
